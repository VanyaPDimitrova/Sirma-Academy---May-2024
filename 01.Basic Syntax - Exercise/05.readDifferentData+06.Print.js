function readDifferentData(name, integer, number) {
  console.log(name + `\n` + integer + `\n` + number);
}

readDifferentData("Ivan", 100, 3.14);
readDifferentData("George", 200, 6.00);
readDifferentData("Mary", 1, 12.12);
